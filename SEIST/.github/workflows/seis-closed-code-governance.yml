name: SEIS CLOSED CODE Governance

on:
  push:
    branches:
      - main
      - UIXAppTTR
  pull_request:
    branches:
      - main
      - UIXAppTTR

jobs:
  governance:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Check SEIS CLOSED CODE files
        run: node scripts/check-seis-closed-code.mjs
